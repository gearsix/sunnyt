{
  "name": "sunnyt",
  "version": "1.1.0",
  "description": "Fetching & parsing Sunset & Sunrise times from sunset-sunrise.org",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --outDir dist/ src/*.ts && cp src/output.mst ./config.json ./dist",
    "start": "npm run build && cd ./dist; node ./index.js"
  },
  "author": "Alex Collins",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.7.4",
    "typescript": "^4.4.2"
  }
}
